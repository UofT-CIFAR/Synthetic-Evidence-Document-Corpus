Faker>=24.0
pyarrow>=15.0
PyYAML>=6.0
Pillow>=10.0
piexif>=1.1.3
pikepdf>=8.0
pytesseract>=0.3.10
opencv-python>=4.8
requests>=2.31
# CORD-v2 source track (`sec/sources/cord.py`) loads naver-clova-ix/cord-v2 via HF.
datasets>=2.16
# Optional adapter dependencies: installed but only imported when the matching
# variant is actually invoked. You can pin narrower versions in a deployment
# lockfile.
openai>=1.40
google-genai>=0.3
# WeasyPrint is used when T4 rendering is routed through HTML/CSS rather than
# the Pillow renderer. It brings system dependencies (cairo, pango) so it is
# optional here.
weasyprint>=61
